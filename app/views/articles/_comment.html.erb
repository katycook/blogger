<div>
  <h4> Comment by <%= comment.author_name %> </h4>
  <p> 
    <%= distance_of_time_in_words(comment.article.created_at, comment.updated_at) %> later
  </p>
  <p>
    <%= distance_of_time_in_words(Time.now, comment.updated_at) %> ago
  </p>
  <p class="comment"><%= comment.body %> </p>
</div>